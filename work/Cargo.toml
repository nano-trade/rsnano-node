[package]
name = "rsnano_work"
version = "0.1.0"
edition = "2021"

[features]
opencl = ["dep:ocl"]

[dependencies]
rsnano_core = { path = "../core" }
tracing = "0"
ocl = { version = "0.19", optional = true }
rand = { version = "0.9" }
serde_json = "1"
