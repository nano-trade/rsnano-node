[package]
name = "rsnano_websocket_client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio-tungstenite = "0"
tokio = { version = "1", features = [
    "net",
] }
futures-util = { version = "0", features = ["sink"] }
